# compiled output
/dist
/node_modules

# Project uses Yarn 4 (packageManager in package.json); npm lock causes drift vs yarn.lock
package-lock.json

# Yarn Berry (do not commit unplugged deps or cache)
.yarn/cache
.yarn/unplugged
.yarn/install-state.gz
.yarn/build-state.yml

# logs
logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# OS
.DS_Store

# tests
/coverage
/.nyc_output

# IDEs
/.idea
.project
.classpath
.c9/
*.launch
.settings/
*.sublime-workspace

# env
.env
.env.local
.env.*.local
